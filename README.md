# Self-Healing-API
ML-powered API security scanner that detects vulnerabilities (SQLi, IDOR, XSS, broken auth &amp; more) in OpenAPI/Postman specs and auto-generates production-ready Python fixes. Built with PyTorch BiLSTM + GRU models, FastAPI backend, and Streamlit UI. OWASP Top-10 coverage.
